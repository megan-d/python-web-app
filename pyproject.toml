[project]
name = "python-web-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "starlette>=0.44.0",
    "uvicorn>=0.33.0",
]
